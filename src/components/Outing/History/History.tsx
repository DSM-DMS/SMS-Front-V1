import React, { FC, ReactElement } from 'react';

import * as S from '../style';

interface Props {}

const History: FC<Props> = (): ReactElement => {
  return <S.HistoryWarp>History</S.HistoryWarp>;
};

export default History;
