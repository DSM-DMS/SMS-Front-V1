import React, { FC, ReactElement } from 'react';

import * as S from '../style';

interface Props {}

const Apply: FC<Props> = (): ReactElement => {
  return <S.ApplyWarp>Apply</S.ApplyWarp>;
};

export default Apply;
