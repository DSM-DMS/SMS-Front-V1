import React, { FC, ReactElement } from 'react';

import { OutingApply } from '../../components';

interface Props {}

const ApplyContainer: FC<Props> = (): ReactElement => {
  return <OutingApply />;
};

export default ApplyContainer;
